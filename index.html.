<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Casino Geibert</title>
    <style>
        body { background: #000; color: #ffd700; text-align: center; font-family: sans-serif; overflow: hidden; }
        .slot-container { border: 10px double #ffd700; padding: 20px; border-radius: 20px; display: inline-block; margin-top: 50px; background: #111; }
        .rodillos { font-size: 70px; margin: 20px; background: #fff; color: #000; border-radius: 10px; padding: 10px; min-width: 250px; }
        button { background: linear-gradient(#ffd700, #b8860b); border: none; padding: 20px 40px; font-size: 25px; font-weight: bold; border-radius: 50px; cursor: pointer; box-shadow: 0 5px #666; }
        button:active { box-shadow: 0 2px #666; transform: translateY(4px); }
        #status { font-size: 20px; margin-top: 20px; color: #fff; }
    </style>
</head>
<body>
    <h1>🎰 MI CASINO PROPIO 🎰</h1>
    <div class="slot-container">
        <div id="display" class="rodillos">🍒|🍒|🍒</div>
        <button onclick="spin()">¡GIRAR!</button>
    </div>
    <p id="status">Costo por giro: 1,000 Bs</p>

    <script>
        const iconos = ["🍒", "🍋", "🔔", "💎", "7️⃣", "💀"];
        function spin() {
            // Lógica de "Casa Gana": 80% de las veces sale algo diferente
            let r1 = iconos[Math.floor(Math.random() * iconos.length)];
            let r2 = iconos[Math.floor(Math.random() * iconos.length)];
            let r3 = iconos[Math.floor(Math.random() * iconos.length)];
            
            document.getElementById('display').innerText = `${r1}|${r2}|${r3}`;
            
            if(r1 === r2 && r2 === r3) {
                document.getElementById('status').innerText = "¡GANASTE! 🎉 (+10,000 Bs)";
                document.getElementById('status').style.color = "lime";
            } else {
                document.getElementById('status').innerText = "Perdiste. Sigue intentando.";
                document.getElementById('status').style.color = "red";
            }
        }
    </script>
</body>
</html>
